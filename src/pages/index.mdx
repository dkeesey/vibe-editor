---
layout: ../layouts/MdxLayout.astro
title: Welcome to Vibe Editor
description: MDX-based inline editing for modern websites
microtext:
  hero-headline: Create magic with every click
  hero-subhead: Click any text. Edit in place. Ship *instantly*.
  cta-primary: Try It Now
  cta-secondary: Learn More
  features:
    - title: Inline Editing
      desc: No more switching between preview and code. Just **click** and type.
    - title: MDX Powered
      desc: Full component flexibility. *Tailwind* styling. React islands.
    - title: AI Ready
      desc: 'Same interface for humans and AI. Generate, edit, iterate.'
  testimonial-text: This is how websites *should* work. Simple but **powerful**.
  testimonial-author: — A Happy Developer
  footer-tagline: Made with **vibes**
---

import MicroText from '../components/MicroText.astro'

export const icons = ['✏️', '🧩', '🤖', '⭐', '🚀', '💡']
export const colors = ['bg-blue-100', 'bg-purple-100', 'bg-green-100', 'bg-yellow-100', 'bg-pink-100', 'bg-orange-100']

<section class="py-24 px-6 text-center bg-gradient-to-b from-blue-50 to-white">
  <div class="max-w-3xl mx-auto">
    <MicroText
      id="hero-headline"
      as="h1"
      class="text-5xl md:text-6xl font-bold text-gray-900 tracking-tight"
    />
    <MicroText
      id="hero-subhead"
      as="p"
      class="mt-6 text-xl md:text-2xl text-gray-600"
    />

    <div class="mt-10 flex gap-4 justify-center flex-wrap">
      <a
        href="?edit"
        class="px-8 py-4 bg-blue-600 text-white font-semibold rounded-lg hover:bg-blue-700 transition-colors shadow-lg shadow-blue-600/20"
      >
        <MicroText id="cta-primary" />
      </a>
      <a
        href="#features"
        class="px-8 py-4 border-2 border-gray-300 text-gray-700 font-semibold rounded-lg hover:border-gray-400 transition-colors"
      >
        <MicroText id="cta-secondary" />
      </a>
    </div>
  </div>
</section>

<section id="features" class="py-20 px-6">
  <div class="max-w-5xl mx-auto">
    <div class="grid md:grid-cols-3 gap-8">
      {frontmatter.microtext.features.map((feature, i) => (
        <div class="p-6 rounded-xl bg-gray-50 hover:bg-gray-100 transition-colors" key={i}>
          <div class={`w-12 h-12 ${colors[i % colors.length]} rounded-lg flex items-center justify-center text-2xl mb-4`}>
            {icons[i % icons.length]}
          </div>
          <MicroText
            id={`features.${i}.title`}
            as="h3"
            class="text-xl font-semibold text-gray-900"
          />
          <MicroText
            id={`features.${i}.desc`}
            as="p"
            class="mt-2 text-gray-600"
          />
        </div>
      ))}
    </div>
  </div>
</section>

<section class="py-16 px-6 bg-gray-900">
  <div class="max-w-2xl mx-auto text-center">
    <MicroText
      id="testimonial-text"
      as="blockquote"
      class="text-2xl md:text-3xl text-white font-light italic"
    />
    <MicroText
      id="testimonial-author"
      as="cite"
      class="mt-4 block text-gray-400 not-italic"
    />
  </div>
</section>

<footer class="py-8 px-6 text-center text-gray-500">
  <MicroText id="footer-tagline" as="p" />
</footer>
